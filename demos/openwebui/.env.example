# Open WebUI demo — API key shared between Hermes and Open WebUI.
# Copy to .env and set a long random value.

# Same key must be set with:
#   hermes config set api_server.key "$HERMES_API_KEY"
# Generate one with: openssl rand -hex 32
HERMES_API_KEY=
